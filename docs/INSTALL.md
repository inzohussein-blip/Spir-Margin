# تثبيت Spir-Margin على حاسوب الشركة

يعمل Spir-Margin من برنامج على الحاسوب نفسه، وقاعدة بياناته على الحاسوب نفسه.
لذلك **لا يحتاج إنترنت** للعمل اليومي — الإنترنت يلزم فقط أثناء التثبيت (لتنزيل
المكوّنات)، وللمزامنة مع قاعدة مستضافة إن ضُبطت.

---

## ما يلزم قبل البدء

- حاسوب بنظام **ويندوز 10 أو 11**، وعليه نحو 2 غيغابايت فارغة.
- **الإنترنت أثناء التثبيت فقط.**
- **Node.js** بالنسخة LTS (18.18 أو أحدث) من <https://nodejs.org>: نزّل ملف `.msi`
  لويندوز واضغط «Next» في كل الشاشات دون تغيير شيء.

---

## ويندوز

### 1. ضع المجلّد في مكانه الدائم

إن وصلك البرنامج ملفاً مضغوطاً: زر أيمن ← «استخراج الكل»، ثم انقل المجلّد إلى
`C:\Spir-Margin`، بحيث يكون `install-windows.cmd` داخله مباشرةً.

> **البيانات تُحفظ داخل هذا المجلّد.** لا تضعه على سطح المكتب ولا في «المستندات»
> (كثيراً ما يزامنهما OneDrive فيُفسد القاعدة)، ولا في «التنزيلات»، ولا على فلاشة أو
> قرص شبكي. المثبِّت يرفض هذه الأماكن.

### 2. التثبيت

انقر نقراً مزدوجاً على **`install-windows.cmd`**.

- إن ظهرت شاشة «حمى Windows جهاز الكمبيوتر»: «مزيد من المعلومات» ← «تشغيل على أي حال».
- النافذة السوداء تعرض التقدّم — **لا تغلقها**؛ المرّة الأولى تأخذ بضع دقائق.

يتولّى المثبِّت كل شيء: يتحقّق من Node.js ومن مكان المجلّد ومن أن المنفذ 3000 غير
مشغول ببرنامج آخر، ويزيل علامة «نُزّل من الإنترنت» عن ملفّات التشغيل (وإلا سأل
ويندوز عنها عند كل تشغيل)، وينزّل المكوّنات ويبني البرنامج، ثم:

| يضيف | يفعل |
| --- | --- |
| أيقونة **Spir-Margin** على سطح المكتب وفي قائمة ابدأ | تشغّل البرنامج إن كان متوقّفاً، وتنتظر جاهزيّته، ثم تفتحه في نافذة مستقلّة |
| تشغيلاً تلقائياً عند تسجيل الدخول إلى ويندوز | يعمل في الخلفية بلا نوافذ، ويعيد تشغيل نفسه إن توقّف |

لا يحتاج صلاحيّات المسؤول.

**قاعدة البيانات تبدأ فارغة**، جاهزة لبيانات الشركة الحقيقية. للتدريب أولاً
على بيانات تجريبية ثبّت بالخيار `-Demo` (على حاسوب تدريب، لا على حاسوب العمل):

```bat
install-windows.cmd -Demo
```

هذا الاختيار يخصّ **قاعدة بيانات جديدة** فقط؛ إن وُجدت بيانات سابقة على الحاسوب
بقيت كما هي، ولا يغيّرها أي خيار.

> لكتابة أمر: افتح المجلّد في مستكشف الملفات، وانقر شريط العنوان، واكتب `cmd`
> واضغط Enter — تُفتح نافذة الأوامر داخل المجلّد.

### 3. أول دخول

1. ادخل بـ `admin@spir.local` وكلمة المرور `123`.
2. **الإعدادات**: اسم الشركة وشعارها وعنوانها — تظهر على كل ورقة مطبوعة.
3. **المستخدمون**: حساب باسم كل موظّف، بدل الحساب المشترك
   (سجلّ التغييرات يحفظ بريد من غيّر كل شيء). من نسي كلمة مروره يعيد المسؤول
   تعيينها من الصفحة نفسها.
4. **المختبرات** و**الأصناف**، ثم **المخزون**: الكِتّات تُباع من دُفعات مُستلَمة،
   فسجّل الاستلام قبل أول بيع — البرنامج يرفض بيع ما ليس في المخزون.

للتأكّد من التثبيت: أعد تشغيل الحاسوب، وانتظر دقيقة، ثم افتح الأيقونة.

### التحديث إلى نسخة جديدة

1. خذ نسخة احتياطية من **الإعدادات ← النسخ الاحتياطي والاستعادة**، واحفظها خارج الحاسوب.
2. انسخ **محتويات** النسخة الجديدة إلى داخل المجلّد نفسه، واختر «استبدال الملفات».
   **لا تحذف المجلّد القديم**: فيه `.pglite-data` (البيانات) و`.env.local` (مفتاح الجلسات).
3. شغّل من نافذة الأوامر داخل المجلّد:

   ```bat
   install-windows.cmd -Update
   ```

   يوقف البرنامج، ويعيد البناء، ثم يشغّله من جديد.

### النقل إلى حاسوب جديد

نسخة احتياطية على القديم ← تثبيت عادي على الجديد ← **الإعدادات ← النسخ الاحتياطي
والاستعادة ← استعادة**. لا تنسخ `.env.local` بين حاسوبين.

### ربط أكثر من حاسوب

من صفحة **الإعداد ← المزامنة** (للمسؤول)، برمز يُنسخ من حاسوب ويُلصق في آخر:

- **داخل المكتب:** في حاسوب يبقى مشغّلاً اضغط «اجعل هذا الحاسوب الرئيسي»، ثم انسخ رمزه
  والصقه في الحواسيب الأخرى. الاتصال عبر شبكة المكتب مباشرةً ومشفّر بالرمز، ولا يلزم
  إنترنت. الحاسوب الجديد يأخذ نسخة كاملة؛ والذي فيه عمل سابق يُدمج عمله.
- **بين الفروع:** اربط حاسوباً واحداً بالقاعدة المستضافة (Session pooler، المنفذ
  5432)، ثم انسخ رمزها من الصفحة نفسها إلى حواسيب الفروع.
- **الاثنان:** الحاسوب الرئيسي في المكتب يرتبط بالقاعدة المستضافة، فينقل عمل المكتب
  إلى الفروع وعملها إليه.

> في المرّة الأولى قد يسأل ويندوز على الحاسوب الرئيسي هل يسمح لـ Node.js بالشبكة —
> اختر **السماح** للشبكات الخاصة. المنفذ الافتراضي للمزامنة 3310، والبرنامج نفسه يبقى
> مغلقاً أمام الشبكة. يجب أن تعمل الحواسيب المربوطة بالنسخة نفسها من البرنامج.

### منفذ آخر

إن كان برنامج آخر يستخدم المنفذ 3000 يتوقّف المثبِّت ويقول ذلك. ثبّت على منفذ آخر:

```bat
install-windows.cmd -Port 3001
```

### الإزالة

```bat
install-windows.cmd -Uninstall
```

يزيل الأيقونات والتشغيل التلقائي ويوقف البرنامج. **لا يحذف البيانات**؛ تبقى في
المجلّد `.pglite-data` إلى أن تحذفه بنفسك.

---

## لينكس

```bash
sudo ./scripts/service/install-linux.sh              # المنفذ 3000
sudo PORT=8080 ./scripts/service/install-linux.sh    # منفذ آخر
sudo SPIR_SEED=demo ./scripts/service/install-linux.sh  # قاعدة جديدة ببيانات تجريبية (للتدريب)
```

يبني البرنامج إن لم يكن مبنيّاً (باسم المستخدم لا باسم الجذر)، ويسجّله خدمةً في
systemd تبدأ مع الحاسوب وتعيد تشغيل نفسها إن توقّفت، ويضيف **Spir-Margin** إلى
قائمة التطبيقات لفتحه في نافذة مستقلّة.

```bash
systemctl status spir-margin        # الحالة
journalctl -u spir-margin -f        # السجلّ
sudo systemctl disable --now spir-margin && sudo rm /etc/systemd/system/spir-margin.service   # الإزالة
```

---

## تثبيته تطبيقاً من المتصفّح (اختياري)

الأيقونة التي يضيفها المثبِّت تكفي. ومن يفضّل تثبيت التطبيق من المتصفّح يجده في
**الإعدادات ← التثبيت كتطبيق** (في Edge وChrome). يعمل التطبيق المثبَّت من
البرنامج نفسه، فلا فرق في البيانات بين الطريقتين.

---

## إن لم يُفتح البرنامج

- **بعد تشغيل الحاسوب مباشرة** قد يحتاج دقيقة ليبدأ. إن فُتحت النافذة قبله تظهر
  صفحة «البرنامج لا يستجيب بعد»، وهي تتّصل بنفسها وتفتح البرنامج حين يجهز.
- افتح أيقونة **Spir-Margin** — فهي تشغّله إن كان متوقّفاً.
- سجلّ الأخطاء في ويندوز: `logs\spir-margin.log` داخل مجلّد البرنامج.
  وفي لينكس: `journalctl -u spir-margin`.

---

## الأمان

- **يستمع البرنامج لهذا الحاسوب فقط** (`127.0.0.1`)، فلا تصل إليه الأجهزة الأخرى
  على الشبكة. هذا مقصود: البرنامج يأتي بحساب مسؤول ثابت
  (`admin@spir.local`) يعمل دون قاعدة بيانات، ولو فُتح للشبكة لاستطاع أي جهاز
  عليها الدخول به.
- لكل تثبيت **مفتاح جلسات خاص به** يُولَّد تلقائياً في الملف `.env.local` عند أول
  بناء. لا تشاركه ولا تنسخه إلى حاسوب آخر؛ حذفه يعني فقط أن يسجّل الجميع
  دخولهم من جديد.
- لفتحه لشبكة المكتب عمداً — على شبكة موثوقة فقط:

  ```bat
  install-windows.cmd -Lan
  ```

  ```bash
  sudo HOST=0.0.0.0 ./scripts/service/install-linux.sh
  ```

---

## البيانات والنسخ الاحتياطي

البيانات كلّها في المجلّد **`.pglite-data`** داخل مجلّد البرنامج. من دون قاعدة
مستضافة مضبوطة، **هذا المجلّد هو النسخة الوحيدة**: خذ نسخة احتياطية بانتظام من
**الإعدادات ← النسخ الاحتياطي والاستعادة**، واحفظها خارج هذا الحاسوب.

التواريخ تُحسب بتوقيت الحاسوب (بغداد). لتثبيت منطقة زمنية أخرى اضبط المتغيّر
`SPIR_TIMEZONE`، مثل `Asia/Baghdad`.
