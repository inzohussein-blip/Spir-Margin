node_modules
.next
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.pglite-data/
tests/browser/artifacts/

# Server log written by scripts/windows/run-server.cmd
logs/

# automatic backups (Settings → Automatic backups)
/backups/
RESET-ADMIN-PASSWORD*

# Updates (Settings → Updates): the updater's work folder, and the release
# number that comes inside each release zip — never committed.
/updates/
/version.json
